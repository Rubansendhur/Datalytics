import { Routes } from '@angular/router';
import { AppComponent } from './app.component'; // Use your actual components

export const routes: Routes = [
  { path: '', component: AppComponent }, // Example route
];
